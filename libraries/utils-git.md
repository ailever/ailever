## Git

### Global Config
`~/.gitconfig`  


`list`
```bash
$ git config --list
```

`registry`
```bash
$ git config --global user.name "user"
$ git config --global user.email "user@domain"
```



### Local Config
`./.git/config`

```bash
$ git remote -v
```
